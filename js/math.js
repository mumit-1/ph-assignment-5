const mainBalance = getValue('balance');
const firstSecBalance = getValue('first-sec-donate');
const secondSecBalance = getValue('second-sec-donate');
const thirdSecBalance = getValue('third-sec-donate');

console.log(mainBalance,firstSecBalance,secondSecBalance,thirdSecBalance);

